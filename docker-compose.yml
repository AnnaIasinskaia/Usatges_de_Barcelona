version: '3.8'

services:
  usatges-de-barcelona:
    image: nvcr.io/nvidia/pytorch:25.05-py3
    container_name: usatges-de-barcelona
    ports:
      - "8888:8888"
      - "6006:6006"
    volumes:
      - type: bind
        source: .
        target: /workspace/Usatges_de_Barcelona
      - type: bind
        source: /mnt/wslg/.X11-unix
        target: /tmp/.X11-unix
    working_dir: /workspace/Usatges_de_Barcelona
    stdin_open: true
    tty: true
    environment:
      - DISPLAY=:0
      - NVIDIA_VISIBLE_DEVICES=all
      - NVIDIA_DRIVER_CAPABILITIES=all
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
    command: bash